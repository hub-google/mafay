import os

# ==========================================
# 系統設定 (請依據您的電腦環境修改)
# ==========================================

# Tesseract-OCR 執行檔的安裝路徑
# 預設為一般安裝路徑，如果您的安裝路徑不同，請修改這裡
TESSERACT_CMD_PATH = r"C:\Program Files\Tesseract-OCR\tesseract.exe"

# ==========================================
# 監控設定
# ==========================================

# 關鍵字設定 (可以使用正則表達式，或簡單的字串比對)
KEYWORD_COUNTDOWN = ["即將開啟", "倒數", "绝境战场即将开启", "絕境戰場即將開啟"]
KEYWORD_MAFEI = ["馬匪", "马匪"]

# OCR 掃描頻率 (秒) - 設定太低會佔用過多 CPU，建議 0.5 到 1 秒
SCAN_INTERVAL_SECONDS = 0.5

# 辨識語言 (chi_sim=簡體中文, chi_tra=繁體中文)
# 由於遊戲內都是簡體字，限定 chi_sim 可以提升速度與準確度
TESSERACT_LANG = "chi_sim"

# ==========================================
# 區域設定 (Region)
# ==========================================
# 這會由 region_selector.py 自動寫入與更新，手動修改請小心
# 支援多個區域設定
REGION_FILE = "region_config.json"

# ==========================================
# 進階視覺設定 (Computer Vision - Template Matching)
# ==========================================
# 存放地圖圖示樣板的資料夾名稱
TEMPLATE_DIR = "templates"

# 樣板比對的相似度閾值 (0.0 ~ 1.0)
# 0.8 代表需要 80% 以上的相似度才判定為發現目標
TEMPLATE_THRESHOLD = 0.8

# ==========================================
# 快捷鍵設定
# ==========================================
# 啟動與暫停監控的快捷鍵
HOTKEY_TOGGLE = "f8"

# 重新設定監控區域的快捷鍵
HOTKEY_RESELECT = "f7"
